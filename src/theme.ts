/**
 * Theme is provided by GCThemeProvider from @eva/gc-design-system.
 * Do NOT instantiate a bare FluentProvider — GCThemeProvider wraps it.
 *
 * In component makeStyles calls, import tokens directly:
 *   import { tokens, makeStyles } from "@fluentui/react-components";
 *
 * GC design tokens are accessible via the same Fluent token system
 * because GCThemeProvider applies the GC overrides to FluentProvider.
 */
export type { Theme } from "@fluentui/react-components";
